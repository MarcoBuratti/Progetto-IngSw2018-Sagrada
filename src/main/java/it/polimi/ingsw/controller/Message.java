package it.polimi.ingsw.controller;

/*7iimport it.polimi.ingsw.model.Die;
import it.polimi.ingsw.model.GameBoard;
import it.polimi.ingsw.model.Player;

import java.util.ArrayList;
import java.util.List;
import java.util.Optional;

public class Message {




    rivate Player player;
    private GameBoard gameboard;
    private int row;
    private int column;
    private Optional<Integer> indexDieOnDraftPool;
    private Optional<Die> die;

    public PlacementMove(String input, it.polimi.ingsw.model.Player player, GameBoard gameBoard) {
        this.player = player;
        this.gameboard = gameBoard;
        String[] scanner= input.split(" ");
        List<String> parameterList = new ArrayList<>();
        for (String s : scanner) {
            if(!s.equals("")) parameterList.add(s);
        }
        this.row=Integer.parseInt(scanner[0]);
        this.column=Integer.parseInt(scanner[1]);
        this.indexDieOnDraftPool=Integer.parseInt(scanner[2]);
    }

    public PlacementMove(String input, Die die,Player player, GameBoard gameBoard){



    }
}
*/